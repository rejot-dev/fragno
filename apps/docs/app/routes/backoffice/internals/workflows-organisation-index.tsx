export default function BackofficeWorkflowsOrganisationIndex() {
  return (
    <div className="text-sm text-[var(--bo-muted)]">
      Select a workflow instance to review its current step, state, and history.
    </div>
  );
}
