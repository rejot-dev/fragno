```typescript title="lib/stripe-client.ts" tab="React"
import { createStripeFragmentClient } from "@fragno-dev/stripe/react";

export const stripeClient = createStripeFragmentClient();
```

```typescript title="lib/stripe-client.ts" tab="Vue"
import { createStripeFragmentClient } from "@fragno-dev/stripe/vue";

export const stripeClient = createStripeFragmentClient();
```

```typescript title="lib/stripe-client.ts" tab="Solid JS"
import { createStripeFragmentClient } from "@fragno-dev/stripe/solid";

export const stripeClient = createStripeFragmentClient();
```

```typescript title="lib/stripe-client.ts" tab="Svelte"
import { createStripeFragmentClient } from "@fragno-dev/stripe/svelte";

export const stripeClient = createStripeFragmentClient();
```

```typescript title="lib/stripe-client.ts" tab="Vanilla"
import { createStripeFragmentClient } from "@fragno-dev/stripe/vanilla";

export const stripeClient = createStripeFragmentClient();
```
