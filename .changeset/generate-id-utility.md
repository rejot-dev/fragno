---
"@fragno-dev/db": patch
---

feat: add `generateId(schema, tableName)` utility for pre-generating table IDs without creating
records. Also available as `TypedUnitOfWork.generateId(tableName)` convenience method.
