{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": ".",
    "composite": true
  },
  "include": ["src"],
  // Not sure why handlers.test.ts causes type checking to hang indefinitly
  "exclude": ["node_modules", "dist", "src/webhook/handlers.test.ts"]
}
