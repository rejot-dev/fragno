# resend-fragment

Fragno fragment package.

## Build

```bash
npm run types:check
npm run build
```

## Next Steps

- Update the fragment definition in `src/definition.ts`
- Define routes in `src/routes.ts`
- Adjust the database schema in `src/schema.ts` if needed
- Add framework-specific clients in `src/client/`
