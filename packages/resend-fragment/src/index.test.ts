import { describe, test } from "vitest";

describe.skip("resend-fragment legacy tests", () => {
  test.skip("migrated to split test files", () => {
    // Intentionally skipped: tests were split across focused domain files.
  });
});
