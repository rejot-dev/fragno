{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "dist/**",
        ".next/**",
        "!.next/cache/**",
        ".output/**",
        ".nuxt/**",
        ".react-router/**",
        ".output/**"
      ]
    },
    "types:check": {
      "dependsOn": ["^types:check"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
