# Keys are only needed to refresh the recorded trace with --capture.
# Set only the keys you use. Auto-selection prefers OpenAI, then Anthropic, then Google.
# OPENAI_API_KEY=your-openai-api-key
# ANTHROPIC_API_KEY=your-anthropic-api-key
# GEMINI_API_KEY=your-gemini-api-key
