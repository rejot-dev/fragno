# Upload example environment

# Proxy mode (filesystem storage).
# Store proxy uploads outside the repo (default: ~/.fragno/upload-example).
UPLOAD_PROXY_DIR=/Users/you/.fragno/upload-example

# Direct mode (S3-compatible storage).
UPLOAD_S3_BUCKET=uploads
UPLOAD_S3_ENDPOINT=http://localhost:9000
UPLOAD_S3_REGION=us-east-1
UPLOAD_S3_ACCESS_KEY_ID=minioadmin
UPLOAD_S3_SECRET_ACCESS_KEY=minioadmin
UPLOAD_S3_PATH_STYLE=true
# Optional: set multipart threshold bytes (default: max single upload size).
# UPLOAD_S3_MULTIPART_THRESHOLD_BYTES=6291456
# Optional: tune multipart part size (bytes).
# UPLOAD_S3_MULTIPART_PART_SIZE_BYTES=5242880
# Optional: force direct uploads to use single upload below this size (bytes).
# UPLOAD_S3_DIRECT_UPLOAD_THRESHOLD_BYTES=2097152
# Optional: override upload expiry (seconds).
# UPLOAD_S3_UPLOAD_EXPIRES_IN_SECONDS=10
# Optional: override signed URL expiry (seconds).
# UPLOAD_S3_SIGNED_URL_EXPIRES_IN_SECONDS=60
# Optional: override size limits (bytes).
# UPLOAD_S3_MAX_SINGLE_UPLOAD_BYTES=1048576
# UPLOAD_S3_MAX_MULTIPART_UPLOAD_BYTES=20971520
# Optional: enforce metadata size (bytes).
# UPLOAD_S3_MAX_METADATA_BYTES=512

# Optional: override proxy upload expiry (seconds).
# UPLOAD_PROXY_UPLOAD_EXPIRES_IN_SECONDS=10

# Optional: enable HTTPS for Vite.
# UPLOAD_EXAMPLE_HTTPS=1
